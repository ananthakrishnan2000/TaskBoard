[build]
builder = "nixpacks"

[[services]]
name = "backend"
startCommand = "cd backend && npm start"

[[static]]
name = "frontend"
source = "frontend"
buildCommand = "cd frontend && npm install && npm run build"
output = "build"