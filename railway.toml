[build]
builder = "nixpacks"

[[services]]
name = "api"
source = "backend"
startCommand = "npm start"

[[static]]
name = "web"
source = "frontend"
buildCommand = "npm install && npm run build"
output = "build"